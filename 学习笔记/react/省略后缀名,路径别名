在webpack的 resolve中配置 extensions 省略后缀名

resolve: {
    extensions: ['.js','.jsx','.css','.json']
}

在webpack的 resolve中配置alias 路径别名
 resolve: {
    alias: {
        '@': path.join(__dirname,'./src')
    }
}