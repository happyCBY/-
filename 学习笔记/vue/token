token字符串包含三种信息

1.加密方式
2.用户数据
3.个人签名


===============
token使用方式
1. 客户端使用ajax向服务器请求login接口，后台校验如果账号和密码是对的，就返回一个token值
2. 客户端在success方法当中拿到token值之后，就立即把token值存起来(sessionStorage)
3. 接下来所有的需要登录之后才能去请求的接口，我们必须在headers当中通过Authorization来发送一个请求头
4. 服务器在拿到请求之后，会先把token判断有没有，然后再判断是否是合法的，如果是合法就把接口数据返回，如果不合法，就返回403


sessionStorage.setItem
sessionStorage.getItem
sessionStorage.clear